# 20. feat: 작업 현황 스탭 권한 레이아웃 적용

- **예정 커밋 메시지**: `feat: 작업 현황 스탭 권한 레이아웃 적용`
- **작업일**: 2026-07-22
- **작업 브랜치**: `작업-현황-스탭-레이아웃` (워크트리 `.claude/worktrees/staff-task-status`, base: main `f368d11`)

---

## 1. 작업 내용 요약

스탭(STAFF) 권한 로그인 시 작업 현황 화면이 Figma **`Task Status Layout — Staff`(87:1029)** 프레임 전체와 일치하도록 마무리했다. 마스터(MASTER)는 현행 유지(사용자 명시).

콘텐츠 영역(필터·요약·플랫 로드맵)은 docs/14에서 이미 권한 분기가 구현·검증되어 있었고, 남아 있던 갭은 **셸 사이드바** — 스탭 로그인 시에도 마스터형(그룹 트리)이 나오던 것(docs/14 §5에 후속 과제로 기록됨). 이번 태스크에서 사이드바 스탭 변형(`Y.OS Shell — Projects · Staff`, 컴포넌트 79:48)을 구현해 프레임 전체가 정합됐다.

## 2. 디자인 소스

- Figma `jimmy-design-system` (fileKey `Vmv6OlcsEHpHnf5vNDVDoX`)
- `Task Status Layout — Staff` 프레임 `87:1029` → 셸 인스턴스 `87:1030` (컴포넌트 `79:48`)
- 컴포넌트 설명: "Y.OS Shell 변형 — 프로젝트 모듈 · 직원 권한 · 디폴트. **소속 그룹 플랫 리스트 + 인라인 프로젝트 추가 고스트 행 (그룹 추가 없음)**"
- 스탭 워크스페이스 그룹: 작업 현황 · 내 작업 · 작업 분석 · **프리셋**(160:499)

## 3. 변경 파일 내역

| 구분 | 파일 | 내용 |
|---|---|---|
| 수정 | `src/components/layout/projects-nav.tsx` | ① 스탭 워크스페이스에 `프리셋` 항목 추가(`STAFF_WORKSPACE_ITEMS`, 아이콘 `SlidersHorizontal`, href `/projects/presets` 자리표시) ② 스탭 프로젝트 섹션을 그룹 트리 대신 **배정 프로젝트 플랫 리스트**(색 점 + 이름, 선택 시 `bg-muted`)로 분기 ③ 스탭에도 `+ 프로젝트 추가` 인라인 고스트 행 노출 — 추가 대상 그룹은 배정 프로젝트의 소속 그룹(스탭 소속 그룹) |

## 4. 주요 결정

| 항목 | 결정 | 이유 |
|---|---|---|
| 배정 프로젝트 소스 | `STAFF_ASSIGNED_PROJECT_IDS` 상수 재사용 | 작업 현황 콘텐츠(docs/14)와 동일 소스 — 배정(멤버) 도메인 도입 시 함께 실데이터화 |
| 스탭 프로젝트 추가 대상 그룹 | 배정 프로젝트가 속한 첫 그룹(=Soft) | 디자인 설명 "소속 그룹 플랫 리스트 + 인라인 프로젝트 추가" — 스탭은 자기 소속 그룹에만 추가 |
| 프리셋을 스탭에만 추가 | 마스터 디자인(84:11)에도 프리셋이 있으나 **"마스터는 현행 유지" 지시**에 따라 스탭만 반영 | 마스터 반영은 별도 태스크로 (아래 후속 과제) |
| 컨텍스트 메뉴(그룹/프로젝트 삭제) | 스탭 미노출 유지 | 기존 `isMaster` 가드 그대로 |
| 스타일 | 기존 마스터 프로젝트 행 클래스 재사용(rounded-[8px], 13px medium, 8px 점) | 코드베이스 관례 우선 — 디자인 노드 값(6px 라운딩)과의 시각 차이는 기존 행들과 동일 기준 |

## 5. 검증

1. `npm run lint` ✓ · `npm run build` ✓ (Next.js 16.2.11, prebuild `prisma generate` 훅 정상)
2. **브라우저 실로그인 검증** (puppeteer `--no-save` 임시 설치, dev 3005 — 3002는 다른 작업자 사용 중, 계정별 독립 브라우저 컨텍스트, 자동 체크 10건 ALL PASS):
   - **step01(스탭)**: 워크스페이스에 프리셋 노출 ✓ / 그룹 행·그룹 추가 미노출(Lab·Printing 없음) ✓ / 배정 3개 프로젝트 플랫 노출·와이즈 제외 ✓ / `+ 프로젝트 추가` 고스트 행 + 인라인 입력 열림 ✓ / 페이지 에러 0 ✓
   - **master01(마스터)**: 그룹 트리(Lab·Soft·Printing) 유지 ✓ / 프리셋 없음(현행 유지) ✓ / 그룹 추가 버튼 유지 ✓ / 페이지 에러 0 ✓
   - 스크린샷 — 스탭 화면이 87:1029 프레임(사이드바 플랫 리스트 + 콘텐츠)과 일치 확인
3. 검증용 puppeteer는 `--no-save`로 설치해 package.json/lock 변경 없음, 검증 스크립트는 스크래치패드 보관(커밋 제외)

## 6. 알려진 사항 / 후속 과제

- **마스터 디자인(84:11) 사이드바에도 `프리셋` 항목이 있음** — 이번엔 지시에 따라 미반영. 마스터에도 반영하려면 `WORKSPACE_ITEMS`에 프리셋을 옮기는 1줄 수준 후속 태스크
- `프리셋`(/projects/presets)·`내 작업`·`작업 분석` 라우트는 자리표시 링크 (페이지 미구현 — 기존과 동일)
- 스탭 `+ 프로젝트 추가`는 로컬 스토어에만 반영 (보드 스토어 체계 그대로) — 서버 도메인 도입 시 실데이터화
- 스탭 배정은 여전히 상수 자리표시 — 멤버/배정 도메인 태스크에서 대체

## 7. 사후 검증 결과 (추록)

push(PR #15) 이후 확정된 결과:

- Vercel 프리뷰 배포 **pass** (Deployment has completed) — https://y-os-core-git-project-hosting-center.vercel.app (한글 브랜치명이라 git 슬러그가 비어 도메인이 짧음)
- 프리뷰 스모크: `/login` **200** · 비로그인 `/projects` **302 리다이렉트** (인증 가드 라이브)
- 머지 후 프로덕션 배포 결과는 요청 사이클 7단계에서 확인 (규칙상 다음 브랜치 문서에 기록)
- **최신 main 반영 (머지 직전, 2회)**: ① PR #13(작업 상세 오버레이)·#14(단계 상세 오버레이) 유입 — main에 docs/17 2개 중복. 반영 후 lint ✓ · build ✓ · 브라우저 자동 체크 10건 ALL PASS 재확인. ② 곧이어 PR #12(프로젝트 상세 정합) 유입 — docs/18 선점. 규칙(나중에 머지되는 쪽이 조정)에 따라 이 브랜치에서 최종 정리: 단계 상세 **17**(유지) · 프로젝트 상세 정합 **18**(main 확정) · 작업 상세 오버레이 17→**19** · 본 문서 17→**20**. 2차 반영 후 lint ✓ · build ✓ 재확인 (유입 코드는 프로젝트 상세 화면 한정 — 본 태스크의 사이드바와 무관)
