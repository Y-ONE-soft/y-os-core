# 13. 단계 추가 다이얼로그 세션 사용자 전환 (빌드 복구)

- **예정 커밋 메시지**: `fix: 단계 추가 다이얼로그 세션 사용자 전환 (빌드 복구)`
- **작업일**: 2026-07-22
- **작업 브랜치**: `세션-전환-빌드-수정`
- **선행 커밋(같은 브랜치)**: `docs: 문서 번호 충돌 조정 (로그인·로그아웃 11→12)` — PR #6(보드 단계·작업 추가)이 11번을 먼저 머지해, 나중에 머지된 로그인·로그아웃 문서를 12번으로 조정.

---

## 1. 무슨 일이 있었나 (첫 시맨틱 병렬 충돌)

**git 충돌 없이 main 빌드가 깨진 첫 사례.**

- PR #6 `feat: 보드 단계·작업 추가 동작 구현` — `stage-add-dialog.tsx`가 `CURRENT_USER`(자리표시 상수)를 import (main에 먼저 머지)
- PR #7 `feat: 세션 기반 로그인·로그아웃 기능 구현` — 인증 도입으로 `CURRENT_USER`를 **삭제**하고 세션 실데이터로 대체 (나중에 머지)
- 두 PR은 서로 다른 파일을 건드려 **GitHub 머지 충돌 없음** → 그러나 머지된 main에서는 존재하지 않는 export를 import → **Turbopack 빌드 실패 → 프로덕션 배포 실패** (기존 배포가 계속 서빙되어 서비스 중단은 없었음)
- 각 브랜치의 프리뷰 배포는 둘 다 success — 병렬 머지의 조합 상태는 어느 쪽 프리뷰도 검증하지 않는다는 것이 핵심 교훈

## 2. 수정 내용

| 구분 | 파일 | 내용 |
|---|---|---|
| 수정 | `src/components/features/projects/stage-add-dialog.tsx` | `CURRENT_USER` import 제거 → `useSession()`으로 세션 사용자 이니셜(`user?.name.charAt(0)`) 표시 — user-menu·projects-nav와 동일 패턴 |
| 수정 | `CLAUDE.md` | 요청 사이클에 **7단계 신설**: "머지 후 프로덕션 배포 success 확인까지가 사이클 완료, 실패 시 즉시 핫픽스" |
| 이름변경 | `docs/11-…로그인-로그아웃…` → `docs/12-…` | 번호 충돌 조정 (선행 커밋) |
| 신규 | `docs/13-fix-단계-추가-다이얼로그-세션-사용자-전환-빌드-복구.md` | 이 문서 |

세션 컨텍스트는 `(main)` 레이아웃의 `SessionProvider`가 제공하므로 이 다이얼로그(프로젝트 상세 하위)에서 바로 사용 가능하다.

## 3. 검증

1. `npm run lint` ✓ · `npm run build` ✓ — 머지된 main + 이 수정 조합에서 빌드 복구 확인
2. 프로덕션 배포 success 및 인증 가드(비로그인 `/` → `/login` 307) 동작은 머지 후 "사후 검증 결과 (추록)"로 기록 예정

## 4. 재발 방지

- CLAUDE.md 요청 사이클 7단계(프로덕션 배포 확인 의무)로 이번 유형의 파손을 **머지 직후 감지**하도록 규칙화
- 근본 원인 유형: 한쪽이 공용 export를 제거/변경 + 다른 쪽이 그 export의 새 사용처 추가. 공용 모듈(`src/lib/constants.ts` 등)을 바꾸는 PR은 머지 직전 `git log origin/main`으로 새 유입 커밋의 사용처를 한 번 훑는 것을 권장
